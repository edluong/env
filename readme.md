# Environment Bash Install Script


Need to give permission to shell file: 
```console
chmod +x <shell_script>.sh
```
Need to add python.vim into ~/.vim/ftplugin/ (create the directories if they dont exist)
